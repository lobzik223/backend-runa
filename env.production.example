# ============================================
# PRODUCTION ENVIRONMENT CONFIGURATION
# ============================================
# Скопируй этот файл в .env.production и заполни реальными значениями
# cp env.production.example .env.production
# nano .env.production

# Server Configuration
NODE_ENV=production
PORT=3000
API_PREFIX=/api

# Database Configuration
# ВАЖНО: Используй СИЛЬНЫЙ пароль для продакшена!
POSTGRES_DB=runa
POSTGRES_USER=runa
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE_MIN_32_CHARS

# Database URL (автоматически формируется из переменных выше)
# DATABASE_URL будет переопределён в docker-compose.prod.yml

# JWT Secrets (ОБЯЗАТЕЛЬНО: сгенерируй уникальные секреты!)
# Генерация: openssl rand -base64 32
JWT_ACCESS_SECRET=CHANGE_ME_GENERATE_32_CHARS_MINIMUM
JWT_REFRESH_SECRET=CHANGE_ME_GENERATE_32_CHARS_MINIMUM
JWT_ACCESS_TTL_SECONDS=900
JWT_REFRESH_TTL_SECONDS=2592000

# Security
# Укажи конкретные домены вместо * для продакшена
# Например: https://api.runa-finance.com,https://app.runa-finance.com
CORS_ORIGIN=https://your-frontend-domain.com
THROTTLE_TTL_SECONDS=60
THROTTLE_LIMIT=30

# Redis Configuration
# ВАЖНО: Установи пароль для Redis в продакшене!
REDIS_PASSWORD=CHANGE_ME_REDIS_PASSWORD
# REDIS_URL будет переопределён в docker-compose.prod.yml

# Application Key (защита от случайных запросов)
# Генерация: openssl rand -hex 16
APP_KEY=CHANGE_ME_GENERATE_APP_KEY_MIN_8_CHARS

# OpenAI (optional, for AI chat)
OPENAI_API_KEY=sk-proj-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
OPENAI_MODEL=gpt-4o-mini

# Tinkoff Invest (optional, for investment features)
TINKOFF_DEMO_TOKEN=your_tinkoff_sandbox_token_here
